# 🎬 Genius Video AI

ИИ-генератор видео в духе Runway Gen-3 / Gen-4.5: **опишите идею и получите фильм нужной длины** (от 15 секунд до 10 минут).

```
идея ─► 🧠 ИИ-режиссёр (сценарий) ─► 🎨 ключевые кадры (Flux)
     ─► 🎥 ИИ-видеомодель снимает каждую сцену (Gen-4.5 / Veo 3.1 / Seedance 2.0 / Wan)
     ─► 🎬 монтаж точно в заданную длину ─► MP4
```

## Движки видео

| Движок | Модели | Ключ |
| --- | --- | --- |
| 🎥 **Pollinations** | Seedance 2.0, Veo 3.1 Fast, Wan 2.7 / 3.0, Nova Reel, Grok Video | `POLLINATIONS_KEY`: регистрация бесплатная, есть еженедельные бесплатные кредиты |
| 🚀 **Runway** | Gen-4.5, Gen-4 Turbo, Veo 3.1 Fast, Seedance 2 | `RUNWAYML_API_SECRET`, платно (у Gen-4.5 около 12 кредитов за секунду) |
| 🖼️ **Анимация кадров** | ИИ-кадры, оживлённые движением камеры | ключ не нужен |

Один ИИ-клип длится 2–15 секунд (зависит от модели). Поэтому длинное видео собирается как фильм: число сцен
рассчитывается по формуле `ceil((длина − переход) / (клип − переход))`, все сцены снимаются параллельно, затем
склеиваются с переходами, цветокоррекцией, субтитрами и музыкой **ровно в заданную длину**.

Ключи прописываются в `.env` (см. `.env.example`) или вводятся в интерфейсе через «⚙️ Ключи». Во втором случае они хранятся только
в браузере и передаются вашему серверу в момент генерации.

## Возможности

- **Раскадровка от ИИ.** По идее генерируются заголовок, сцены, субтитры на языке пользователя и подробные промпты на английском.
- **Кадры от ИИ.** Модель Flux через [Pollinations.ai](https://pollinations.ai), стили на выбор: кино, фото, аниме, 3D-мульт, акварель, киберпанк, фэнтези, пиксель-арт.
- **Режим «Оживить мои фото»:** каждое фото становится стартовым кадром, и ИИ его оживляет. Можно вставлять и свои видеоклипы.
- **Кинематографичный монтаж:**
  - плавное движение камеры (эффект Кена Бёрнса: наезд, отъезд, панорамы);
  - переходы: растворение, через чёрный, зум, сдвиг, шторка;
  - цветокоррекция: кино, сочный, тёплый, холодный, винтаж с зерном, нуар;
  - анимированные субтитры и заголовок.
- **Саундтрек.** Встроенный синтезатор с пятью настроениями или ваш аудиофайл.
- **Форматы:** 16:9 (YouTube), 9:16 (Shorts/TikTok/Reels), 1:1, 4:5.
- **Редактор раскадровки:** правка подписей и промптов, перерисовка кадра, перестановка, удаление и замена сцен.
- **Галерея** готовых видео.
- **Работа без интернета.** Если сервисы ИИ недоступны, включается шаблонный сценарий и встроенный процедурный художник, так что видео получится всегда.

## Запуск

```bash
./run.sh            # создаст .venv, поставит зависимости, запустит сервер на :8000
# или
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

Откройте http://localhost:8000. Системный ffmpeg не нужен: он подтягивается пакетом `imageio-ffmpeg`.

## Как устроено

| Часть | Файл | Что делает |
| --- | --- | --- |
| Интерфейс | `static/` | Vanilla JS. Сценарий и кадры запрашиваются у Pollinations прямо из браузера (CORS разрешён) |
| API | `app/main.py` | FastAPI: `/api/clip` (ИИ-клип), `/api/render` (монтаж), `/api/jobs/{id}`, `/api/plan`, `/api/videos` |
| Видеомодели | `app/providers.py` | Pollinations `/video` и Runway `image_to_video` / `text_to_video` с опросом задачи |
| Рендер | `app/render.py` | Кадры собираются в Pillow/NumPy с субпиксельной камерой и передаются в ffmpeg (H.264 + AAC) |
| Музыка | `app/music.py` | Процедурный синтез: пэды, бас, арпеджио, задержка |
| Офлайн-художник | `app/procedural.py` | Рисует сцену по ключевым словам промпта |

Рендеры выполняются по очереди, по одному. Ролик из 5 сцен в 720p собирается за 10–20 секунд на 2 ядрах CPU.

## Лицензии

Шрифт DejaVu лежит в `assets/fonts/`, его лицензия в `LICENSE-DejaVu.txt`.
