name: Build Money Work for Windows

on:
  push:
    branches:
      - arena/01a0dce0-xiaomi-kernel-opensource
    paths:
      - 'money-work/**'
      - '.github/workflows/money-work-windows.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  windows-installer:
    runs-on: windows-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Python for the MT5 bridge
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build read-only MT5 connector
        working-directory: money-work
        shell: pwsh
        run: |
          python -m pip install --upgrade pip pyinstaller MetaTrader5 numpy
          pyinstaller --clean --noconfirm --onefile --collect-all MetaTrader5 --collect-all numpy --collect-submodules numpy --hidden-import=numpy._core._multiarray_umath --name mt5-bridge --distpath bridge/dist --workpath bridge/build bridge/mt5_bridge.py
          if (!(Test-Path "bridge/dist/mt5-bridge.exe")) { throw "PyInstaller did not produce bridge/dist/mt5-bridge.exe" }

      - name: Smoke test bundled MT5 connector imports NumPy and MT5
        working-directory: money-work
        shell: pwsh
        run: |
          $reply = '{"action":"status","requestId":1}' | & "bridge/dist/mt5-bridge.exe"
          if ($LASTEXITCODE -ne 0) { throw "MT5 bridge import smoke test failed with exit code $LASTEXITCODE. Output: $reply" }
          $response = ($reply | Select-Object -Last 1) | ConvertFrom-Json
          if ($response.type -ne "response" -or $response.ok -ne $true -or $response.connected -ne $false) {
            throw "Unexpected MT5 bridge smoke-test response: $reply"
          }

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: money-work/package-lock.json

      - name: Verify app version
        working-directory: money-work
        shell: pwsh
        run: |
          $version = (Get-Content VERSION -Raw).Trim()
          $package = (Get-Content package.json -Raw | ConvertFrom-Json).version
          if ($version -ne $package) { throw "VERSION ($version) differs from package.json ($package)" }
          "version=$version" >> $env:GITHUB_OUTPUT
        id: version

      - name: Install app dependencies for tests
        working-directory: money-work
        run: |
          npm ci
          if ($LASTEXITCODE -ne 0) { throw "npm ci failed" }
        shell: pwsh

      - name: Run JavaScript agent tests
        working-directory: money-work
        run: |
          npm run test:js
          if ($LASTEXITCODE -ne 0) { throw "Money Work JavaScript tests failed" }
        shell: pwsh

      - name: Run Python trading-risk and mocked MT5 order tests
        working-directory: money-work
        run: |
          python -m unittest discover -s tests -p "test_*.py" -v
          if ($LASTEXITCODE -ne 0) { throw "Money Work Python bridge/risk tests failed" }
        shell: pwsh

      - name: Verify public AUD/CAD reference endpoint
        shell: pwsh
        run: |
          $reference = Invoke-RestMethod -Uri "https://api.frankfurter.dev/v1/latest?base=AUD&symbols=CAD" -TimeoutSec 30 -Headers @{ Accept = "application/json" }
          if ($reference.base -ne "AUD" -or !$reference.rates -or !($reference.rates.CAD -gt 0)) { throw "The public AUD/CAD reference response failed schema/rate checks." }
          $sourceDate = [DateTime]::ParseExact($reference.date, "yyyy-MM-dd", [Globalization.CultureInfo]::InvariantCulture)
          if ($sourceDate.Date -gt [DateTime]::UtcNow.Date) { throw "The public AUD/CAD reference date is in the future: $($reference.date)" }
          Write-Host "Verified AUD/CAD daily reference $($reference.rates.CAD) published $($reference.date)"

      - name: Install and build Windows installer
        working-directory: money-work
        run: |
          npm run dist:win
          if ($LASTEXITCODE -ne 0) { throw "Windows installer build failed" }
        shell: pwsh

      - name: Verify MT5 connector is inside the unpacked app
        working-directory: money-work
        shell: pwsh
        run: |
          if (!(Test-Path "release/win-unpacked/resources/mt5-bridge.exe")) {
            throw "Installer payload is missing release/win-unpacked/resources/mt5-bridge.exe"
          }

      - name: Publish versioned download
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          APP_VERSION: ${{ steps.version.outputs.version }}
        run: |
          $tag = "money-work-v$env:APP_VERSION"
          $installer = "money-work/release/Money-Work-Setup-$env:APP_VERSION.exe"
          $existing = gh release list --json tagName --jq '.[].tagName' | Where-Object { $_ -eq $tag }
          if ($existing) {
            gh release upload $tag $installer --clobber
          } else {
            gh release create $tag $installer --title "Money Work v$env:APP_VERSION" --notes "Money Work v$env:APP_VERSION adds Bybit MT5 CFD server presets, interactive AUD/CAD strategy catalog, draggable 2,000-candle MT5 chart with candle/line, zoom and SMA/EMA controls, and an animated three-agent dashboard with the account balance centered over a realistic Earth graphic. The brain and miner mascots toggle their respective agents, each agent has a settings control, and reduced-motion preferences are respected. Paper stops at 80,000,000 CAD virtual equity; MT5 latches off when balance or equity reaches 80,000,000 units of its account currency (no conversion) and closes only bot-owned positions. The runner retains the 0.01-lot cap, mandatory SL/TP, one-position rule, 1% account-wide daily-loss stop, live confirmation, and adaptive safeguards; cash targets do not guarantee profit or override safety exits. Windows CI runs analyst/reference and mocked broker/risk tests, connector runtime imports, checks the public reference endpoint, and verifies the installer before publishing." --prerelease
          }

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: Money-Work-Windows-Installer
          path: money-work/release/Money-Work-Setup-${{ steps.version.outputs.version }}.exe
          if-no-files-found: error
          retention-days: 14
